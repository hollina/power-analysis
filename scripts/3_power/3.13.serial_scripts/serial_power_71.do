
st_cntrl_only_power_analysis_ddd, ///
        end_value(.12) ///
        step_size(.005) ///
        max_dataset_number(1000) ///
        first_year(2003) ///
        last_year(2013) ///
        death_type(amen) ///
        dem_type(none) ///
        list_of_controls(yes) ///
        weight_var(attpop) /// 
        cluster_var(state) ///
        prefix(ln)

