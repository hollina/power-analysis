
st_power_analysis_ddd, ///
    end_value(.12) ///
    step_size(.005) ///
    max_dataset_number(1000) ///
    first_year(2001) ///
    last_year(2010) ///
    death_type(amen) ///
    dem_type(white_non_hisp) ///
    list_of_controls(yes) ///
    weight_var(attpop) /// 
    cluster_var(state) ///
    prefix(ln)
